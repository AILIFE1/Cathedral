# Cathedral Memory Service v2 — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Rate limiting
slowapi==0.1.9

# Structured logging
structlog==24.4.0

# Prometheus metrics
prometheus-client==0.21.0

# Pydantic (bundled with FastAPI but pinned for stability)
pydantic==2.9.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2     # required by FastAPI TestClient
